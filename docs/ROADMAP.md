# Roadmap

## Phase 0 — Foundation ✅ (this session)

Next.js 16 + TS + Tailwind v4 scaffold; MDX + zod content layer; Work / Writing /
About routes; ⌘K palette; d3 + React Flow island examples; RSS / sitemap / robots.
Builds clean and fully static.

## Phase 1 — Design (next — Claude design session)

Work from [DESIGN-BRIEF.md](./DESIGN-BRIEF.md). Establish the visual language and
the long-form reading layout. Fill the design tokens; style prose and all
components.

## Phase 2 — Content

Write 2–3 real case studies and the first posts. Build the bespoke islands each
writeup needs (d3 / React Flow / custom). Add cover images.

## Phase 3 — Enhancements

- Per-post Open Graph images (`next/og`)
- Content search (the palette is already the hook)
- Tag pages
- Optional live widgets (e.g. GitHub activity) via an OpenAPI-contracted proxy
  route — **no DB**
- Vercel Analytics + Speed Insights

## Phase 4 — Launch

- Connect `nick-buser/nickbuser.com` to a Vercel project (team *Nick's projects*)
- Attach `nickbuser.com` + `www`; set the canonical redirect
- Verify metadata / OG / RSS; pass Lighthouse
- Ship 🚀
